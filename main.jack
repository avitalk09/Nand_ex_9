class main {
    // attributes and methods

    function void main() {
        var Graphic graphic;
        var RPSGame game;


        // 
        do Random.init();

        // Create the objects
        let graphic = Graphic.new();
        let game = RPSGame.new(graphic);

        do game.run();

        // Start the game 
        do game.AnnounceWinner();

        do game.dispose();

        return;
    }
}